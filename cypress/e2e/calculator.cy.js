/// <reference types="cypress" />

describe("Calculator User Stories", () => {
  beforeEach(() => {
    cy.visit("http://localhost:3000");
  });

  it("User can access calculator", () => {
    cy.visit("https://example.cypress.io");
  });

  it("User with a misdemeanor that can be vacated", () => {
    cy.visit("https://example.cypress.io");
  });

  it("User with a misdemeanor that cannot be vacated", () => {
    cy.visit("https://example.cypress.io");
  });

  it("User looking for resources for jobs, housing, and education", () => {
    cy.visit("https://example.cypress.io");
  });
});


