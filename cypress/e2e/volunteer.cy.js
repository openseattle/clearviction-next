describe("Volunteer User Stories", () => {
  beforeEach(() => {
    cy.visit("http://localhost:3000");
  });

  it("User can visit get-involved page", () => {
    cy.visit("https://example.cypress.io");
  });

  it("User can learn about ways to contirbute and view open volunteer roles", () => {
    cy.visit("https://example.cypress.io");
  });

  it("User can apply for open roles", () => {
    cy.visit("https://example.cypress.io");
  });

  it("User can contact organization to ask questions about getting involved", () => {
    cy.visit("https://example.cypress.io");
  });
});
